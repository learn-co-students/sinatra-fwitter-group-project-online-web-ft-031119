<!doctype html>
<html>
  <head>
    <title>Fwitter</title>
  </head>
  <body>

    <div class="container">
      <% user_name = "" %>
      <% if @current_user %>
        <% user_name = @current_user.username %>
      <% end %>
      <h1>Welcome <%=user_name+" "%> to Fwitter!</h1>
      <h2>Home of the Fweet.</h2>

      <%= yield %>

      <br><br>

      <% path = request.path_info %>

      <% if @current_user %>
        <% if path != '/new' %><a href="/tweets/new">New Fweet</a><% end %>
        <% if path != '/homepage' %><a href="/homepage">Homepage</a><% end %>
        <a href="/logout">Log Out</a>
      <% else %>
        <% if path != '/login' && path != '/signup' %><a href="/login">Log In</a><% end %>
        <% if path != '/login' && path != '/signup' %><a href="/signup">Sign Up</a><% end %>
      <% end %>
      <% if path != '/' %><a href="/">See it all</a><% end %>
    </div>

  </body>
</html>
